name: Test Runner
on:
        branches: [master]
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Hello from Pi
        run: echo "Hello from Raspberry Pi runner!"
